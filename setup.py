from setuptools import setup, find_packages

setup(
    name="agentic-context-manager",
    version="0.1.0",
    description="Deterministic memory layer for AI agents",
    author="Jaya Kumar",
    packages=find_packages(),
    install_requires=[],
)

